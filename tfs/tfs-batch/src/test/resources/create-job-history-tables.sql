CREATE TABLE "JOB_HISTORY"
(
   ID INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),
   JOB_NAME varchar(255),
   TRIGGER_NAME varchar(255),
   GROUP_NAME varchar(255),
   START_TIME timestamp,
   END_TIME timestamp,
   PREVIOUS_FIRE_TIME timestamp,
   NEXT_FIRE_TIME timestamp,
   STATUS varchar(20),
   MESSAGE VARCHAR(255)
)
;

CREATE TABLE "JOB_EXCEPTIONS"
(
   JOB_NAME varchar(255),
   TRIGGER_NAME varchar(255),
   GROUP_NAME varchar(255),
   ERROR_MESSAGE varchar(255),
   LOGTIME timestamp
)
;

--INSERT INTO JOB_HISTORY (JOB_NAME,TRIGGER_NAME,GROUP_NAME,START_TIME,PREVIOUS_FIRE_TIME,NEXT_FIRE_TIME) VALUES (?,?,?,?);
--INSERT INTO JOB_EXCEPTIONS (JOB_NAME,TRIGGER,GROUP_NAME,ERROR_MESSAGE) VALUES (?,?,?,?)
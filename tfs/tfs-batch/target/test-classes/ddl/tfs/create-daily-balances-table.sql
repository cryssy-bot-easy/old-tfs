CREATE TABLE "DAILYBALANCE"
(
   ID bigint PRIMARY KEY NOT NULL,
   DOCUMENTNUMBER varchar(30),
   BALANCE bigint,
   BALANCEDATE timestamp
)
;
CREATE UNIQUE INDEX SQL130213053040600 ON "DAILYBALANCE"(ID)
;

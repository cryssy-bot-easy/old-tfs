CREATE TABLE DOCUMENTSENCLOSED
(
    ID VARCHAR(36) PRIMARY KEY NOT NULL,
    DOCUMENTNAME VARCHAR(500),
    ORIGINAL1 BIGINT,
    ORIGINAL2 BIGINT,
    DUPLICATE1 BIGINT,
    DUPLICATE2 BIGINT,
    DOCUMENTNUMBER VARCHAR(21)
);
CREATE TABLE ENCLOSEDINSTRUCTION
(
    ID VARCHAR(36) PRIMARY KEY NOT NULL,
    INSTRUCTION VARCHAR(3000),
    INSTRUCTIONTYPE VARCHAR(7),
    DOCUMENTNUMBER VARCHAR(21)
);
CREATE TABLE EXPORTBILLS
(
    DOCUMENTNUMBER VARCHAR(21) PRIMARY KEY NOT NULL,
    EXPORTBILLTYPE VARCHAR(3),
    EXPORTVIAPDDTSFLAG SMALLINT,
    CWTFLAG SMALLINT,
    PAYMENTMODE VARCHAR(2),
    OUTSTANDINGAMOUNT DECIMAL(19,2),
    NEGOTIATIONNUMBER VARCHAR(21),
    FACILITYTYPE VARCHAR(20),
    FACILTIYID VARCHAR(20),
    BOOKINGCURRENCY VARCHAR(3),
    LOANAMOUNT DECIMAL(19,2),
    INTERESTRATE DECIMAL(19,2),
    INTERESTTERMCODE VARCHAR(1),
    INTERESTTERM BIGINT,
    REPRICINGTERMCODE VARCHAR(1),
    REPRICINGTERM BIGINT,
    LOANTERMCODE VARCHAR(1),
    LOANTERM BIGINT,
    LOANMATURITYDATE TIMESTAMP,
    NUMBEROFFREEFLOATDAYS BIGINT,
    AGRIAGRATAGGING VARCHAR(7),
    PAYMENTCODE VARCHAR(1),
    PNNUMBER VARCHAR(20),
    TRANSACTIONPOSTINGSTATUS VARCHAR(20),
    ADVISENUMBER VARCHAR(20),
    LCNUMBER VARCHAR(21),
    LCISSUEDATE TIMESTAMP,
    LCTYPE VARCHAR(10),
    LCTENOR VARCHAR(8),
    USANCETERM BIGINT,
    LCCURRENCY VARCHAR(3),
    LCAMOUNT DECIMAL(19,2),
    LCEXPIRYDATE TIMESTAMP,
    ISSUINGBANKCODE VARCHAR(20),
    ISSUINGBANKADDRESS VARCHAR(350),
    REIMBURSINGBANKCODE VARCHAR(20),
    LCDESCRIPTIONOFGOODS CLOB(6500),
    TENOR VARCHAR(2),
    TENORTERM BIGINT,
    DRAFTCURRENCY VARCHAR(3),
    DRAFTAMOUNT DECIMAL(19,2),
    DUEDATE TIMESTAMP,
    COLLECTINGBANKCODE VARCHAR(20),
    COLLECTINGBANKADDRESS VARCHAR(350),
    NONLCDESCRIPTIONOFGOODS CLOB(6500),
    INVOICENUMBER VARCHAR(20),
    ADDITIONALAMOUNT DECIMAL(19,2),
    CHARGESCODE VARCHAR(8),
    CHARGEAMOUNT DECIMAL(19,2),
    TOTALAMOUNTCLAIMEDFLAG VARCHAR(1),
    TOTALAMOUNTCLAIMEDDATE TIMESTAMP,
    TOTALAMOUNTCLAIMED DECIMAL(19,2),
    CORRESBANKCODE VARCHAR(20),
    CORRESBANKNAME VARCHAR(50),
    ACCOUNTTYPE VARCHAR(3),
    DEPOSITORYACCOUNTNUMBER VARCHAR(20),
    GLCODE VARCHAR(20),
    CORRESBANKCURRENCY VARCHAR(3),
    SENDERTORECEIVERINFORMATION VARCHAR(350),
    PROCESSDATE TIMESTAMP,
    SETTLEMENTDATE TIMESTAMP,
	  PROCEEDSAMOUNT DECIMAL(19,2)
);
ALTER TABLE DOCUMENTSENCLOSED ADD FOREIGN KEY ( DOCUMENTNUMBER ) REFERENCES TRADEPRODUCT ( DOCUMENTNUMBER );
ALTER TABLE ENCLOSEDINSTRUCTION ADD FOREIGN KEY ( DOCUMENTNUMBER ) REFERENCES TRADEPRODUCT ( DOCUMENTNUMBER );
ALTER TABLE EXPORTBILLS ADD FOREIGN KEY ( DOCUMENTNUMBER ) REFERENCES TRADEPRODUCT ( DOCUMENTNUMBER );
